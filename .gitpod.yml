image: nfcore/gitpod:latest

tasks:
  - name: Initialize
    init: |
      pip install graphviz
      . /opt/conda/etc/profile.d/conda.sh
      conda activate base
      mamba install -y -q -c bioconda fastp snakemake kraken2 fastq-dl pysradb
